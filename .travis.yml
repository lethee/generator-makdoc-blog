language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-makdoc-blog' ]; then cd .. && eval "mv $currentfolder generator-makdoc-blog" && cd generator-makdoc-blog; fi
